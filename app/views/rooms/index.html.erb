<h1>Rooms#index</h1>
<p>Find me in app/views/rooms/index.html.erb</p>
<%= link_to "Return", user_motels_path(@user) %>

<% @rooms.each do |room| %>
  <%= link_to "#{room.name} bookings", user_motel_bookings_path(@user, @user.motel_id, room) %>
<% end %>
