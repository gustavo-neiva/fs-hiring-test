<% @motels.each do |motel| %>
  <%= link_to "#{motel.name} Rooms", user_motel_rooms_path(@user, @user.motel_id) %>
  <%= link_to "#{motel.name} Bookings", user_motel_bookings_path(@user, @user.motel_id) %>
<% end %>
